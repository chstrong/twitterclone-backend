#foreach ($user in $context.result.data.twitterapp-user-dev)
  #if ($user.id == $context.identity.username)
    #set ($user["__typename"] = "MyProfile")
  #else
    #set ($user["__typename"] = "OtherProfile")
  #end
#end 

$util.toJson($context.result.data.twitterapp-user-dev)